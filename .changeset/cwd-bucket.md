---
"@cvr/counsel": minor
---

Group output artifacts by working directory using a deterministic cwd bucket (`<segments>-<hash>`). Adds `outputBucket` field to both `RunManifest` and `DryRunPreview` stdout payloads.
